spring:
  datasource:
    url: jdbc:h2:mem:structlab-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  jwt:
    secret: structlab-test-secret-that-is-long-enough-for-hs256-testing
    ttl-hours: 1
  cors:
    allowed-origins: http://localhost:3000
